import java.lang.Math;

public class binary {    
    public static void main(String[] args) {
        int base = 10; // put base 10 number tests here 

        System.out.println(base + "in binary is " + Integer.toBinaryString(base));

        /*String binary = "";
        int n = base; 
        while (n > 0) {
        if (n % 2 == 0){
        binary = "0" + binary;
        } else {
        binary = "1" + binary;
        }
        n = n/2;
        }

        System.out.print(binary); 
         */
    }
}